<%= simple_form_for product, url: products_path, method: :post  do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :balance %>

  <%= f.button :submit, "Create product", class: 'btn-submit' %>
<% end %>
