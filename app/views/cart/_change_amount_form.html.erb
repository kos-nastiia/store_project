<%= form_with url: change_amount_product_in_cart_path(product), method: :patch do |f| %>
  <%= f.number_field :amount, min: 1, value: session.dig(:products, product.id.to_s), class:'rounded-lg input-field w-24 h-9 inline-block' %>
  <%= f.submit "Change amount", class: "btn-blue" %>
<% end %>
